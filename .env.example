# Database Configuration
POSTGRES_DB=naturetranquille
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_production

# Backend API Configuration
API_PORT=4000
DATABASE_URL=postgresql://postgres:changeme@localhost:5432/naturetranquille

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_MAPLIBRE_STYLE=https://demotiles.maplibre.org/style.json

# SMTP Configuration (Phase 2 - Feedback emails)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=

# Optional: Production overrides
# NODE_ENV=production
# API_BASE_URL=https://api.naturetranquille.fr
